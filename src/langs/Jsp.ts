/**
 * @file Jsp.ts
 * @description foo
 * @author Jungho
 * @since 2026-1-4
 */

import type { PrettierOptions, StripOptions } from "@exportLibs";
import {
	getPrettier,
	getPrettierPluginJsp,
	htmlMinify,
	strip,
} from "@exportLibs";
import { logger, modal } from "@exportScripts";
import type { CommonType } from "@exportTypes";

// 0. removeComments ―――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――
export const removeComments = async (
	contents: string,
	_fileTabSize: number,
	_fileEol: string,
	fileExt: string,
) => {
	try {
		// 1. `http://` -> `httpp`
		const pattern1 = /("|')(\s*)(http:\/\/)([\S\s]*?)("|')/gm;
		const pattern2 = /("|')(\s*)(https:\/\/)([\S\s]*?)("|')/gm;
		const pattern3 = /("|')(\s*)(@{http:\/\/)([\S\s]*?)("|')/gm;
		const pattern4 = /("|')(\s*)(@{https:\/\/)([\S\s]*?)("|')/gm;

		const httpResult = contents
			.replaceAll(
				pattern1,
				(...p: unknown[]) => `${p[1]}${p[2]}httpp${p[4]}${p[5]}`,
			)
			.replaceAll(
				pattern2,
				(...p: unknown[]) => `${p[1]}${p[2]}httpps${p[4]}${p[5]}`,
			)
			.replaceAll(
				pattern3,
				(...p: unknown[]) => `${p[1]}${p[2]}@{httpp${p[4]}${p[5]}`,
			)
			.replaceAll(
				pattern4,
				(...p: unknown[]) => `${p[1]}${p[2]}@{httpps${p[4]}${p[5]}`,
			);

		const minifyResult = await htmlMinify(httpResult, {
			caseSensitive: true,
			collapseBooleanAttributes: false,
			collapseInlineTagWhitespace: true,
			collapseWhitespace: true,
			conservativeCollapse: true,
			continueOnParseError: true,
			decodeEntities: true,
			html5: true,
			includeAutoGeneratedTags: false,
			keepClosingSlash: true,
			maxLineLength: 0,
			minifyCSS: false,
			minifyJS: false,
			minifyURLs: false,
			noNewlinesBeforeTagClose: false,
			preserveLineBreaks: true,
			preventAttributesEscaping: true,
			processConditionalComments: false,
			processScripts: [],
			quoteCharacter: `"`,
			removeAttributeQuotes: false,
			removeComments: true,
			removeEmptyAttributes: true,
			removeEmptyElements: false,
			removeOptionalTags: false,
			removeRedundantAttributes: false,
			removeScriptTypeAttributes: true,
			removeStyleLinkTypeAttributes: true,
			removeTagWhitespace: false,
			sortAttributes: true,
			sortClassName: true,
			trimCustomFragments: false,
			useShortDoctype: true,
		});

		const baseOptions1: StripOptions = {
			block: true,
			keepProtected: false,
			language: `html`,
			line: true,
			preserveNewlines: false,
		};
		const baseOptions2: StripOptions = {
			block: true,
			keepProtected: false,
			language: `javascript`,
			line: true,
			preserveNewlines: false,
		};
		const baseOptions3: StripOptions = {
			block: true,
			keepProtected: false,
			language: `css`,
			line: true,
			preserveNewlines: false,
		};

		const stripResult1 = strip(minifyResult, baseOptions1);
		const stripResult2 = strip(stripResult1, baseOptions2);
		const stripResult3 = strip(stripResult2, baseOptions3);

		// 2. `httpp` -> `http://`
		const pattern1Re = /("|')(\s*)(httpp)([\S\s]*?)("|')/gm;
		const pattern2Re = /("|')(\s*)(httpps)([\S\s]*?)("|')/gm;
		const pattern3Re = /("|')(\s*)(@{httpp)([\S\s]*?)("|')/gm;
		const pattern4Re = /("|')(\s*)(@{httpps)([\S\s]*?)("|')/gm;
		const pattern5 = /(\n)(\s*)(\n)/gm;

		const finalResult: string = stripResult3
			.replaceAll(
				pattern1Re,
				(...p: unknown[]) => `${p[1]}${p[2]}http://${p[4]}${p[5]}`,
			)
			.replaceAll(
				pattern2Re,
				(...p: unknown[]) => `${p[1]}${p[2]}https://${p[4]}${p[5]}`,
			)
			.replaceAll(
				pattern3Re,
				(...p: unknown[]) => `${p[1]}${p[2]}@{http://${p[4]}${p[5]}`,
			)
			.replaceAll(
				pattern4Re,
				(...p: unknown[]) => `${p[1]}${p[2]}@{https://${p[4]}${p[5]}`,
			)
			.replaceAll(pattern5, (...p: unknown[]) => `${p[1]}`);

		logger(`debug`, `${fileExt}:removeComments - Y`);
		return finalResult;
	}
  catch (error: unknown) {
		logger(`error`, `${fileExt}:removeComments - ${(error as Error).message}`);
		return contents;
	}
};

// 1. prettierFormat ―――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――
export const prettierFormat = async (
	commonParam: CommonType,
	contents: string,
	fileName: string,
	_fileTabSize: number,
	fileEol: string,
	fileExt: string,
) => {
	try {
		logger(`debug`, `${fileExt}:prettierFormat - start`);
		// 0. prettier
		const prettier = await getPrettier();
		const prettierStatus = prettier ? `prettier:loaded` : `prettier:missing`;
		logger(
			prettier ? `debug` : `warn`,
			`${fileExt}:prettierFormat - ${prettierStatus}`,
		);

		// 1. parser
		const parser = `html`;

		// 2. plugin
		const plugin = await getPrettierPluginJsp();
		const pluginStatus = plugin ? `plugin:jsp:loaded` : `plugin:jsp:missing`;
		logger(
			plugin ? `debug` : `warn`,
			`${fileExt}:prettierFormat - ${pluginStatus}`,
		);

		// 3. options
		const baseOptions: PrettierOptions = {
			__embeddedInHtml: true,
			arrowParens: `always`,
			bracketSameLine: false,
			bracketSpacing: true,
			checkIgnorePragma: false,
			embeddedLanguageFormatting: `auto`,
			endOfLine: fileEol === `lf` ? `lf` : `crlf`,
			filepath: fileName,
			htmlWhitespaceSensitivity: `ignore`,
			insertPragma: false,
			jsxBracketSameLine: false,
			jsxSingleQuote: commonParam.quoteType === `single`,
			objectWrap: `preserve`,
			parser: parser,
			plugins: plugin ? [plugin] : [],
			printWidth: 1000,
			proseWrap: `preserve`,
			quoteProps: `as-needed`,
			rangeEnd: Number.POSITIVE_INFINITY,
			rangeStart: 0,
			requirePragma: false,
			semi: true,
			singleAttributePerLine: false,
			singleQuote: commonParam.quoteType === `single`,
			tabWidth: commonParam.indentSize,
			trailingComma: `all`,
			useTabs: commonParam.useTabs,
			vueIndentScriptAndStyle: true,
      experimentalOperatorPosition: "end",
      experimentalTernaries: true,
		};

		const rules1 = /(&nbsp;)/gm;
		const rules2 =
			/(\s*)[^"'`](<.*>)(<)(input|label|a|b|p|span|select|div|option|bold|caption|strong)/gi;

		const result = contents
			.replaceAll(rules1, () => ``)
			.replaceAll(
				rules2,
				(...p: unknown[]) => `${p[1]}${p[2]}\n${p[1]}\t${p[3]}${p[4]}`,
			);
		const formatAvailable = prettier && plugin && typeof prettier.format === `function`;
		logger(
			formatAvailable ? `debug` : `warn`,
			`${fileExt}:prettierFormat - ${formatAvailable ? `formatter:ready` : `formatter:missing`}`,
		);
		const finalResult = formatAvailable
			? await (async () => {
					logger(`debug`, `${fileExt}:prettierFormat - format:start`);
					const formatted = await prettier.format(result, baseOptions);
					const chained = (formatted as string).replaceAll(
						/\n(\t+)(\.)/g,
						(...p: unknown[]) => {
							const p1 = p[1] as string;
							const p2 = p[2] as string;
							return `\n${p1.slice(1)}${p2}`;
						},
					);
					logger(`debug`, `${fileExt}:prettierFormat - format:success`);
					return chained;
				})()
			: (() => {
					logger(`warn`, `${fileExt}:prettierFormat - format:skipped`);
					return contents;
				})();
		logger(`debug`, `${fileExt}:prettierFormat - end`);
		return finalResult;
	}
  catch (error: unknown) {
		const msg = (error as Error).message
			.toString()
			.trim()
			.replaceAll(/\u001B\[[\d;]*[FGKm]/g, ``);
		const msgRegex = /([\S\s]*)(\s*)(https)(.*?)(\()(.*?)(\))([\S\s]*)/gm;
		const msgReplacement = `[Jlint]\n\nError Line = [ $6 ]\nError Site = $8`;
		const msgResult = msg.replaceAll(msgRegex, msgReplacement);

		logger(`error`, `${fileExt}:prettierFormat - ${msgResult}`);
		modal(`error`, `${fileExt}: Prettier Format Error:\n${msgResult}`);
		return contents;
	}
};

// 2. insertLine ―――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――
export const insertLine = async (contents: string, fileExt: string) => {
	try {
		const rules1 = /^(?!\/\/--)\n*(\s*)(<head\s*.*\s*>)(\s*?)/gm;
		const rules2 = /^(?!\/\/--)\n*(\s*)(<body\s*.*\s*>)(\s*?)/gm;
		const rules3 = /^(?!\/\/--)\n*(\s*)(<header\s*.*\s*>)(\s*?)/gm;
		const rules4 = /^(?!\/\/--)\n*(\s*)(<main\s*.*\s*>)(\s*?)/gm;
		const rules5 = /^(?!\/\/--)\n*(\s*)(<footer\s*.*\s*>)(\s*?)/gm;
		const rules6 = /^(?!\/\/--)\n*(\s*)(<section\s*.*\s*>)(\s*?)/gm;
		const rules7 = /^(?!\/\/--)\n*(\s*)(<table\s*.*\s*>)(\s*?)/gm;
		const rules8 = /^(?!\/\/--)\n*(\s*)(<form\s*.*\s*>)(\s*?)/gm;
		const rules9 = /^(?!\/\/--)\n*(\s*)(<div class="row\s*.*\s*>)(\s*?)/gm;

		const finalResult: string = contents
			.replaceAll(rules1, (...p: unknown[]) => {
				const p1 = p[1] as string;
				const p2 = p[2] as string;
				const p3 = p[3] as string;
				const spaceSize = 100 - (p1.length + `<!--`.length + `-`.length);
				const dividerLine = `<!--${`-`.repeat(spaceSize)}-->`;
				return `\n${p1}${dividerLine}\n${p1}${p2}${p3}`;
			})
			.replaceAll(rules2, (...p: unknown[]) => {
				const p1 = p[1] as string;
				const p2 = p[2] as string;
				const p3 = p[3] as string;
				const spaceSize = 100 - (p1.length + `<!--`.length + `-`.length);
				const dividerLine = `<!--${`-`.repeat(spaceSize)}-->`;
				return `\n${p1}${dividerLine}\n${p1}${p2}${p3}`;
			})
			.replaceAll(rules3, (...p: unknown[]) => {
				const p1 = p[1] as string;
				const p2 = p[2] as string;
				const p3 = p[3] as string;
				const spaceSize = 100 - (p1.length + `<!--`.length + `-`.length);
				const dividerLine = `<!--${`-`.repeat(spaceSize)}-->`;
				return `\n${p1}${dividerLine}\n${p1}${p2}${p3}`;
			})
			.replaceAll(rules4, (...p: unknown[]) => {
				const p1 = p[1] as string;
				const p2 = p[2] as string;
				const p3 = p[3] as string;
				const spaceSize = 100 - (p1.length + `<!--`.length + `-`.length);
				const dividerLine = `<!--${`-`.repeat(spaceSize)}-->`;
				return `\n${p1}${dividerLine}\n${p1}${p2}${p3}`;
			})
			.replaceAll(rules5, (...p: unknown[]) => {
				const p1 = p[1] as string;
				const p2 = p[2] as string;
				const p3 = p[3] as string;
				const spaceSize = 100 - (p1.length + `<!--`.length + `-`.length);
				const dividerLine = `<!--${`-`.repeat(spaceSize)}-->`;
				return `\n${p1}${dividerLine}\n${p1}${p2}${p3}`;
			})
			.replaceAll(rules6, (...p: unknown[]) => {
				const p1 = p[1] as string;
				const p2 = p[2] as string;
				const p3 = p[3] as string;
				const spaceSize = 100 - (p1.length + `<!--`.length + `-`.length);
				const dividerLine = `<!--${`-`.repeat(spaceSize)}-->`;
				return `\n${p1}${dividerLine}\n${p1}${p2}${p3}`;
			})
			.replaceAll(rules7, (...p: unknown[]) => {
				const p1 = p[1] as string;
				const p2 = p[2] as string;
				const p3 = p[3] as string;
				const spaceSize = 100 - (p1.length + `<!--`.length + `-`.length);
				const dividerLine = `<!--${`-`.repeat(spaceSize)}-->`;
				return `\n${p1}${dividerLine}\n${p1}${p2}${p3}`;
			})
			.replaceAll(rules8, (...p: unknown[]) => {
				const p1 = p[1] as string;
				const p2 = p[2] as string;
				const p3 = p[3] as string;
				const spaceSize = 100 - (p1.length + `<!--`.length + `-`.length);
				const dividerLine = `<!--${`-`.repeat(spaceSize)}-->`;
				return `\n${p1}${dividerLine}\n${p1}${p2}${p3}`;
			})
			.replaceAll(rules9, (...p: unknown[]) => {
				const p1 = p[1] as string;
				const p2 = p[2] as string;
				const p3 = p[3] as string;
				const spaceSize = 100 - (p1.length + `<!--`.length + `-`.length);
				const dividerLine = `<!--${`-`.repeat(spaceSize)}-->`;
				return `\n${p1}${dividerLine}\n${p1}${p2}${p3}`;
			});

		logger(`debug`, `${fileExt}:insertLine - Y`);
		return finalResult;
	}
  catch (error: unknown) {
		logger(`error`, `${fileExt}:insertLine - ${(error as Error).message}`);
		return contents;
	}
};

// 3. insertSpace ――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――――
export const insertSpace = async (contents: string, fileExt: string) => {
	try {
		const rules1 = /(\s*)(\))(\s+)(;)/gm;
		const rules2 = /(\s*)(@)(\s*)([\S\s]*?)(\s*)(\()/gm;
		const rules3 = /(\s*?)(ception)({)/gm;

		const finalResult: string = contents
			.replaceAll(rules1, (...p: unknown[]) => `${p[1]}${p[2]}${p[4]}`)
			.replaceAll(rules2, (...p: unknown[]) => `${p[1]}${p[2]}${p[4]} ${p[6]}`)
			.replaceAll(rules3, (...p: unknown[]) => `${p[2]} ${p[3]}`);

		logger(`debug`, `${fileExt}:insertSpace - Y`);
		return finalResult;
	}
  catch (error: unknown) {
		logger(`error`, `${fileExt}:insertSpace - ${(error as Error).message}`);
		return contents;
	}
};
